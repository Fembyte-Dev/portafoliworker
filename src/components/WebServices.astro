---
import ServiceCard from './ServiceCard.astro';
---

<div class="services-section">
  <h2 class="section-title">Desarrollo de Sitios Web y Landing Pages</h2>
  
  <div class="services-grid">
    <ServiceCard 
      title="Páginas web adaptables" 
      description="Diseño responsivo que se adapta perfectamente a todos los dispositivos móviles, garantizando una experiencia óptima para tus usuarios." 
      icon="fas fa-mobile-alt"
    />
    
    <ServiceCard 
      title="Diseño UX/UI" 
      description="Interfaces intuitivas y orientadas al usuario que mejoran la experiencia y facilitan la navegación en tu sitio web." 
      icon="fas fa-paint-brush"
    />
    
    <ServiceCard 
      title="Landing pages que convierten" 
      description="Páginas de aterrizaje optimizadas para campañas de marketing, promoción de productos o captación de leads con alto índice de conversión." 
      icon="fas fa-chart-line"
    />
    
    <ServiceCard 
      title="Integraciones avanzadas" 
      description="Implementación de herramientas como Google Analytics, formularios inteligentes, CRM y otras soluciones para potenciar tu presencia digital." 
      icon="fas fa-plug"
    />
  </div>
</div>

<style>
  .services-section {
    margin: 50px 0;
    animation: fadeIn 1s ease-in-out;
  }
  
  .section-title {
    font-size: 2rem;
    margin-bottom: 30px;
    color: var(--text-primary);
    text-align: center;
    position: relative;
    display: inline-block;
  }
  
  .section-title::after {
    content: "";
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 3px;
    background: linear-gradient(90deg, var(--accent-neon-blue), var(--accent-neon-pink));
    border-radius: 3px;
  }
  
  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 25px;
    margin-top: 30px;
  }
  
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  @media (max-width: 768px) {
    .services-grid {
      grid-template-columns: 1fr;
    }
  }
</style>